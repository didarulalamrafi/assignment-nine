import RegisterForm from "./RegisterForm";
export const metadata = {
  title: "Register | MediQueue",
  description: "Create a new MediQueue account",
};

export default function LoginPage() {
  return <RegisterForm />;
}
